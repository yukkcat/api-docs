---
title: Nano Banana
description: Nano Banana 的 OpenAI 兼容模型与 Gemini 官方协议模型调用指南
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

<img src="/gemini-avatar.svg" alt="Nano Banana 插画" width="220" />

本页包含两组模型，但它们的调用协议不同：`nano-banana2` 和 `nano-banana-pro` 走 OpenAI 兼容协议；`gemini-3.1-flash-image-preview` 和 `gemini-3-pro-image-preview` 走 Gemini 官方原生协议。请根据模型选择对应的接口和请求体，不要交叉调用。

## 基础信息

| 项目 | 说明 |
| --- | --- |
| API 根地址 | `https://api.klong.lat` |
| OpenAI SDK Base URL | `https://api.klong.lat/v1` |
| OpenAI 认证 | `Authorization: Bearer <API_KEY>` |
| Gemini 认证 | 推荐使用 `x-goog-api-key: <API_KEY>`，也支持本页列出的其他官方写法 |
| 图片保存 | URL 返回结果可能不是永久存储，获取后请及时转存到自己的对象存储 |

## 模型

| 模型 | 调用协议 | 请求入口 |
| --- | --- | --- |
| `nano-banana2` | OpenAI 兼容协议 | `/v1/chat/completions` 或 `/v1/images/*` |
| `nano-banana-pro` | OpenAI 兼容协议 | `/v1/chat/completions` 或 `/v1/images/*` |
| `gemini-3.1-flash-image-preview` | Gemini 官方协议 | `POST /v1beta/models/{model}:generateContent` |
| `gemini-3-pro-image-preview` | Gemini 官方协议 | `POST /v1beta/models/{model}:generateContent` |

模型名不仅代表能力，也决定调用协议。前两个模型不要放进 Gemini 的 URL，后两个模型不要传给 OpenAI Images API。

## 选择请求格式

| 需求 | 使用方式 |
| --- | --- |
| 在聊天客户端中使用 `nano-banana2` 或 `nano-banana-pro` | `POST /v1/chat/completions` |
| 使用 `nano-banana2` 或 `nano-banana-pro` 文生图 | `POST /v1/images/generations` |
| 使用 `nano-banana2` 或 `nano-banana-pro` 编辑图片 | `POST /v1/images/edits` |
| 使用 `gemini-3.1-flash-image-preview` 或 `gemini-3-pro-image-preview` | `POST /v1beta/models/{model}:generateContent` |
| Gemini 官方协议需要流式响应 | 将方法名改为 `:streamGenerateContent`，请求体保持一致 |

## OpenAI 兼容格式

本节只适用于 `nano-banana2` 和 `nano-banana-pro`。

### Chat Completions

适合已经接入 OpenAI 对话接口、new-api、one-api 或只能使用聊天模型的客户端。

```http
POST https://api.klong.lat/v1/chat/completions
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

```json
{
  "model": "nano-banana-pro",
  "messages": [
    {
      "role": "user",
      "content": "一只戴着巫师帽的橘猫，插画风格"
    }
  ],
  "aspect_ratio": "16:9",
  "image_size": "2K"
}
```

`aspect_ratio` 与 `image_size` 是顶层可选参数，也接受 `aspectRatio` 与 `imageSize`。非流式响应中的图片 URL 位于 `choices[0].message.content`，通常以 Markdown 图片形式返回：

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "![Generated Image](<IMAGE_URL>)"
      },
      "finish_reason": "stop"
    }
  ]
}
```

图生图时，把 `content` 改成数组；`image_url.url` 可以使用公网图片 URL 或 Data URL，也可以依次放入多张参考图：

```json
{
  "model": "nano-banana-pro",
  "messages": [{
    "role": "user",
    "content": [
      { "type": "text", "text": "把图 1 的主体与图 2 的背景合成一张图" },
      { "type": "image_url", "image_url": { "url": "https://your-domain.example/subject.png" } },
      { "type": "image_url", "image_url": { "url": "data:image/png;base64,<BASE64>" } }
    ]
  }],
  "image_size": "2K"
}
```

需要流式返回时加入 `"stream": true`；还需要最后一段返回用量时，再加入 `"stream_options": { "include_usage": true }`。

部分只能填写模型名、无法单独填写分辨率的客户端，可以把档位写进模型名，例如 `nano-banana-pro 4k` 或 `nano-banana2 2k`。

### Images API 文生图

```http
POST https://api.klong.lat/v1/images/generations
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

```json
{
  "model": "nano-banana2",
  "prompt": "木桌上的红苹果，柔和棚拍光线，极简背景",
  "size": "1024x1024",
  "response_format": "url"
}
```

`nano-banana-pro` 的调用方式相同，只需要把 `model` 改成 `nano-banana-pro`。不传 `response_format` 时，服务端默认返回 URL；需要 Base64 时显式传 `b64_json`。

`size` 可以写成像素尺寸（如 `1024x1024`）、宽高比（如 `16:9`）或分辨率档位（如 `2K`）。像素尺寸会由服务端匹配到最接近的宽高比与档位，最终尺寸以返回图片为准。

典型响应如下，图片位于 `data[].url`；选择 Base64 时读取 `data[].b64_json`：

```json
{
  "created": 1787600000,
  "data": [
    { "url": "<IMAGE_URL>" }
  ],
  "usage": {
    "input_tokens": 8,
    "output_tokens": 1124,
    "total_tokens": 1132
  }
}
```

### Images API 图生图

```http
POST https://api.klong.lat/v1/images/edits
Authorization: Bearer <API_KEY>
Content-Type: multipart/form-data
```

```bash
curl -X POST "https://api.klong.lat/v1/images/edits" \
  -H "Authorization: Bearer <API_KEY>" \
  -F "model=nano-banana2" \
  -F "prompt=改成日系水彩风，保留主体和构图" \
  -F "response_format=url" \
  -F "image=@input.png"
```

除了 OpenAI 官方风格的 `multipart/form-data`，也可以用 JSON 提交 URL 或 Data URL。参考图字段支持 `image`，并兼容 `image_url`、`images`、`reference_images`；单张时传字符串，多张时传数组：

```http
POST https://api.klong.lat/v1/images/edits
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

```json
{
  "model": "nano-banana-pro",
  "prompt": "换成夜景背景",
  "image": [
    "https://your-domain.example/a.png",
    "https://your-domain.example/b.png"
  ],
  "size": "2K"
}
```

### OpenAI SDK

<Tabs items={['Python', 'Node.js']}>
<Tab>

```python
from openai import OpenAI

client = OpenAI(
    api_key="<API_KEY>",
    base_url="https://api.klong.lat/v1",
)

result = client.images.generate(
    model="nano-banana2",
    prompt="木桌上的红苹果，柔和棚拍光线，极简背景",
    size="1024x1024",
)

print(result.data[0].url)
```

</Tab>
<Tab>

```javascript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: "<API_KEY>",
  baseURL: "https://api.klong.lat/v1",
});

const result = await client.images.generate({
  model: "nano-banana2",
  prompt: "木桌上的红苹果，柔和棚拍光线，极简背景",
  size: "1024x1024",
});

console.log(result.data[0].url);
```

</Tab>
</Tabs>

## Gemini 原生格式

本节只适用于 `gemini-3.1-flash-image-preview` 和 `gemini-3-pro-image-preview`，请求字段遵循 Gemini 官方协议。

### 请求地址与请求头

```http
POST https://api.klong.lat/v1beta/models/{model}:generateContent
x-goog-api-key: <API_KEY>
Content-Type: application/json
```

把 `{model}` 替换为 `gemini-3.1-flash-image-preview` 或 `gemini-3-pro-image-preview`。比例与分辨率统一放在 `generationConfig.imageConfig` 中。

Gemini 官方协议支持三种认证写法，任选一种即可，同一个请求不需要重复发送：

- `x-goog-api-key: <API_KEY>`（Google SDK 常用）
- `Authorization: Bearer <API_KEY>`
- 在 URL 后添加 `?key=<API_KEY>`

推荐使用上面示例中的 `x-goog-api-key`。文档统一使用 `/v1beta/models/...`；当前服务兼容时也可使用 `/v1/models/...`。

### 原生文生图请求

```json
{
  "contents": [
    {
      "role": "user",
      "parts": [
        { "text": "木桌上的红苹果，柔和棚拍光线，极简背景" }
      ]
    }
  ],
  "generationConfig": {
    "responseModalities": ["IMAGE"],
    "imageConfig": {
      "aspectRatio": "1:1",
      "imageSize": "1K"
    }
  }
}
```

OpenAI 的 `size` 和 `response_format` 不属于 Gemini 原生请求。Gemini 图像参数如下：

| 字段 | 说明 |
| --- | --- |
| `responseModalities` | 返回图片时传 `['IMAGE']`。 |
| `imageConfig.aspectRatio` | 图片比例，例如 `1:1`、`16:9`、`3:4`。不需要固定比例时省略。 |
| `imageConfig.imageSize` | 分辨率档位，例如 `1K`、`2K`、`4K`；以当前模型和服务端支持范围为准。 |

### 返回模式

`responseModalities` 决定响应中希望得到的内容类型：

| 值 | 常见结果 | 说明 |
| --- | --- | --- |
| `['IMAGE']` | `inlineData`（Base64） | 当前文档推荐的图片返回方式。 |
| `['TEXT']` | `fileData.fileUri` 或文本 | 只有服务端启用 URL/文本转换时才可用，不能假设所有部署都支持。 |
| `['IMAGE', 'TEXT']` | 图片和 URL/文本同时返回 | 同样取决于服务端是否启用混合返回。 |

原生图片响应优先读取 `candidates[].content.parts[]`，不要只按数组下标读取：响应中可能先出现文字说明，再出现图片。常用字段如下：

| 响应字段 | 用途 |
| --- | --- |
| `inlineData.mimeType` + `inlineData.data` | 拼接 `data:{mimeType};base64,{data}`，直接显示或保存。 |
| `fileData.fileUri` | 读取服务端返回的图片 URL（如果当前部署支持 URL 返回）。 |
| `text` | 模型返回的文字说明，不一定包含图片。 |

> 当前服务未明确开启 `TEXT` 或混合返回时，请固定使用 `['IMAGE']`，并以 `inlineData` 是否存在判断是否成功。原生请求只写 `imageConfig`；不要添加 OpenAI 的 `response_format` 或 `responseFormat` 字段。

### 比例与分辨率速查

下面是 Gemini 图像模型常见的目标像素，实际结果仍以当前模型和服务端返回的图片尺寸为准：

| 比例 | 1K | 2K | 4K |
| --- | --- | --- | --- |
| `1:1` | 1024×1024 | 2048×2048 | 4096×4096 |
| `16:9` | 1376×768 | 2752×1536 | 5504×3072 |
| `9:16` | 768×1376 | 1536×2752 | 3072×5504 |
| `4:3` | 1200×896 | 2400×1792 | 4800×3584 |
| `3:4` | 896×1200 | 1792×2400 | 3584×4800 |
| `3:2` | 1264×848 | 2528×1696 | 5056×3392 |
| `2:3` | 848×1264 | 1696×2528 | 3392×5056 |
| `5:4` | 1152×928 | 2304×1856 | 4608×3712 |
| `4:5` | 928×1152 | 1856×2304 | 3712×4608 |
| `21:9` | 1584×672 | 3168×1344 | 6336×2688 |

`imageSize` 必须使用大写的 `1K`、`2K` 或 `4K`。`aspectRatio` 和 `imageSize` 都省略时，由上游自行决定；文生图建议显式填写两者。不要传 `aspectRatio: "auto"`，需要自动比例时直接省略字段。

部分上游还提供 `8:1`、`4:1`、`1:4`、`1:8` 这类超宽比例。它们并非所有模型或部署都开放；只有在服务端明确支持并且实际返回成功时才使用，遇到 `400` 时请改用上面的通用比例。

参考实现常见的 1K 尺寸如下；如果服务端按同一比例放大，2K、4K 通常约为下表的 2 倍、4 倍，但最终像素仍以实际响应为准：

| 比例 | 参考 1K 尺寸 |
| --- | --- |
| `8:1` | 2928×352 |
| `4:1` | 2064×512 |
| `1:4` | 512×2064 |
| `1:8` | 352×2928 |

### 原生响应

图片通常位于 `candidates[0].content.parts` 的 `inlineData` 节点：

```json
{
  "candidates": [
    {
      "content": {
        "parts": [
          {
            "inlineData": {
              "mimeType": "image/png",
              "data": "<BASE64>"
            }
          }
        ]
      }
    }
  ]
}
```

### 各语言原生示例

下面的示例都使用 `gemini-3.1-flash-image-preview`。需要使用 Pro 时，只把 URL 中的模型名替换为 `gemini-3-pro-image-preview`；请求体不变。

<Tabs items={['cURL', 'Python', 'Node.js', 'PHP', 'Go', 'Java', 'C#']}>
<Tab>

```bash
curl -X POST "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent" \
  -H "x-goog-api-key: <API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{
      "role": "user",
      "parts": [{ "text": "木桌上的红苹果，柔和棚拍光线，极简背景" }]
    }],
    "generationConfig": {
      "responseModalities": ["IMAGE"],
      "imageConfig": { "aspectRatio": "1:1", "imageSize": "1K" }
    }
  }'
```

</Tab>
<Tab>

```python
import requests

url = "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent"
payload = {
    "contents": [{
        "role": "user",
        "parts": [{"text": "木桌上的红苹果，柔和棚拍光线，极简背景"}],
    }],
    "generationConfig": {
        "responseModalities": ["IMAGE"],
        "imageConfig": {"aspectRatio": "1:1", "imageSize": "1K"},
    },
}

response = requests.post(
    url,
    headers={"x-goog-api-key": "<API_KEY>"},
    json=payload,
)
response.raise_for_status()
print(response.json())
```

</Tab>
<Tab>

```javascript
const response = await fetch(
  "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent",
  {
    method: "POST",
    headers: {
      "x-goog-api-key": "<API_KEY>",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      contents: [{
        role: "user",
        parts: [{ text: "木桌上的红苹果，柔和棚拍光线，极简背景" }],
      }],
      generationConfig: {
        responseModalities: ["IMAGE"],
        imageConfig: { aspectRatio: "1:1", imageSize: "1K" },
      },
    }),
  },
);

console.log(await response.json());
```

</Tab>
<Tab>

```php
<?php
$url = "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent";
$payload = [
    "contents" => [[
        "role" => "user",
        "parts" => [["text" => "木桌上的红苹果，柔和棚拍光线，极简背景"]],
    ]],
    "generationConfig" => [
        "responseModalities" => ["IMAGE"],
        "imageConfig" => ["aspectRatio" => "1:1", "imageSize" => "1K"],
    ],
];

$ch = curl_init($url);
curl_setopt_array($ch, [
    CURLOPT_POST => true,
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER => [
        "x-goog-api-key: <API_KEY>",
        "Content-Type: application/json",
    ],
    CURLOPT_POSTFIELDS => json_encode($payload),
]);

echo curl_exec($ch);
```

</Tab>
<Tab>

```go
package main

import (
    "bytes"
    "fmt"
    "io"
    "net/http"
)

func main() {
    body := []byte(`{"contents":[{"role":"user","parts":[{"text":"木桌上的红苹果，柔和棚拍光线，极简背景"}]}],"generationConfig":{"responseModalities":["IMAGE"],"imageConfig":{"aspectRatio":"1:1","imageSize":"1K"}}}`)
    req, _ := http.NewRequest(
        "POST",
        "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent",
        bytes.NewReader(body),
    )
    req.Header.Set("x-goog-api-key", "<API_KEY>")
    req.Header.Set("Content-Type", "application/json")

    resp, err := http.DefaultClient.Do(req)
    if err != nil { panic(err) }
    defer resp.Body.Close()
    data, _ := io.ReadAll(resp.Body)
    fmt.Println(string(data))
}
```

</Tab>
<Tab>

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class Main {
  public static void main(String[] args) throws Exception {
    String body = """
      {"contents":[{"role":"user","parts":[{"text":"木桌上的红苹果，柔和棚拍光线，极简背景"}]}],"generationConfig":{"responseModalities":["IMAGE"],"imageConfig":{"aspectRatio":"1:1","imageSize":"1K"}}}
      """;

    HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent"))
        .header("x-goog-api-key", "<API_KEY>")
        .header("Content-Type", "application/json")
        .POST(HttpRequest.BodyPublishers.ofString(body))
        .build();

    var response = HttpClient.newHttpClient().send(
        request, HttpResponse.BodyHandlers.ofString());
    System.out.println(response.body());
  }
}
```

</Tab>
<Tab>

```csharp
using System.Net.Http.Json;

using var client = new HttpClient();
client.DefaultRequestHeaders.Add("x-goog-api-key", "<API_KEY>");

var response = await client.PostAsJsonAsync(
    "https://api.klong.lat/v1beta/models/gemini-3.1-flash-image-preview:generateContent",
    new {
        contents = new[] {
            new {
                role = "user",
                parts = new[] {
                    new { text = "木桌上的红苹果，柔和棚拍光线，极简背景" }
                }
            }
        },
        generationConfig = new {
            responseModalities = new[] { "IMAGE" },
            imageConfig = new {
                aspectRatio = "1:1", imageSize = "1K"
            }
        }
    }
);

Console.WriteLine(await response.Content.ReadAsStringAsync());
```

</Tab>
</Tabs>

## Gemini 原生图生图

### Base64 输入

在 `parts` 中同时传文字和 `inlineData`。下方统一使用 Gemini REST 的 camelCase 字段；服务也兼容 `inline_data` / `mime_type` 这类 snake_case 写法：

```json
{
  "contents": [{
    "role": "user",
    "parts": [
      { "text": "改成日系水彩风，保留主体和构图" },
      {
        "inlineData": {
          "mimeType": "image/png",
          "data": "<BASE64>"
        }
      }
    ]
  }],
  "generationConfig": {
    "responseModalities": ["IMAGE"],
    "imageConfig": { "imageSize": "2K" }
  }
}
```

### URL 输入

如果服务端支持远程图片输入，可以使用 `fileData`：

```json
{
  "contents": [{
    "role": "user",
    "parts": [
      { "text": "改成日系水彩风，保留主体和构图" },
      {
        "fileData": {
          "mimeType": "image/png",
          "fileUri": "https://example.com/input.png"
        }
      }
    ]
  }],
  "generationConfig": {
    "responseModalities": ["IMAGE"],
    "imageConfig": { "imageSize": "2K" }
  }
}
```

`inlineData` 和 `fileData` 都是图片 part，可以按 `contents[].parts[]` 中的顺序混合传入；远程 URL 必须能被服务端访问。参考资料常见的“最多 14 张”不是本接口的固定承诺，图片数量上限以当前模型、服务端配置和实际错误响应为准。

省略 `imageConfig.aspectRatio` 时，服务端可以根据参考图决定比例；需要固定比例时再传 `aspectRatio`。

## 错误与使用建议

| HTTP 状态码 | 常见原因 | 建议 |
| --- | --- | --- |
| `400` | 请求字段错误，或内容被安全策略拦截 | 核对协议、模型名、参数与提示词 |
| `401` | API Key 缺失或无效 | 检查认证头，不要同时混用多种认证方式 |
| `402` | 当前账户额度不足 | 补充额度后重试 |
| `429` | 请求过多或触发限流 | 降低并发并稍后重试 |
| `503` / `504` | 上游繁忙或处理超时 | 稍后重试，并保留响应 ID 便于排查 |

图片生成通常比文本请求更久，4K 还会进一步增加耗时。客户端请求超时建议设置为至少 5 分钟。URL 结果获取后请及时转存，避免源链接过期。

## 常见问题

### OpenAI 和 Gemini 请求能混用吗？

不能。OpenAI 兼容入口使用 `model`、`prompt`、`size` 和 `response_format`，并且只调用 `nano-banana2` 或 `nano-banana-pro`；Gemini 原生入口使用 `contents`、`generationConfig.responseModalities` 和 `generationConfig.imageConfig`，并且只调用两个 `gemini-*` 模型。模型名和请求字段都要与入口对应。

### `nano-banana2` 和 `nano-banana-pro` 怎么选？

先使用 `nano-banana2` 验证请求格式和业务流程；需要使用 Pro 模型时把模型名替换为 `nano-banana-pro`，其他 OpenAI 兼容字段保持不变。具体速度、额度和可用尺寸以服务端当前配置为准。

### 为什么看不到图片？

Gemini 官方协议先检查 `candidates[].content.parts[].inlineData.data`，并根据 `mimeType` 拼接 `data:{mimeType};base64,{data}`。OpenAI Images API 读取 `data[0].url` 或 `data[0].b64_json`；Chat Completions 则读取 `choices[0].message.content` 中的 Markdown 图片链接。

长段中文仍可能出现错字、漏字或顺序错误。图片适合做构图和视觉草稿；需要准确排版的标题、条款或正文，建议生成后在设计软件中添加。
